/*
 * Challenge 6: Full Vigenere Cipher Program
 *
 * Goal: Complete interactive Vigenere cipher tool with menu.
 *
 * Menu:
 *   === Vigenere Cipher Tool ===
 *   1. Encrypt a message
 *   2. Decrypt a message
 *   3. Kasiski analysis
 *   4. Exit
 *
 * Features:
 *   - Prompt user for message and key
 *   - Encrypt and display result
 *   - Decrypt and verify round-trip
 *   - Run Kasiski examination on ciphertext
 *   - Loop until user chooses exit
 *
 * Combine all your previous Vigenere functions here:
 *   - repeat_key()
 *   - vigenere_encrypt_char() / vigenere_decrypt_char()
 *   - vigenere_encrypt() / vigenere_decrypt()
 *   - gcd()
 *   - find_repeated_sequences()
 */

#include <stdio.h>
#include <string.h>

// Copy all your functions from previous Vigenere challenges here

int main(void)
{
    // Your code here
    // 1. Display the menu in a loop
    // 2. Read the user's choice
    // 3. For encrypt/decrypt: read message and key, call function, print result
    // 4. For Kasiski: read ciphertext, run analysis, print findings
    // 5. Exit when user chooses option 4
    return 0;
}
