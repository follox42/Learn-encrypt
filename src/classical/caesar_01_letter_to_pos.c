/*
 * Challenge 1: Letter to Position
 *
 * Goal: Convert a letter to its position in the alphabet (0-25)
 *
 * Tests:
 * 'A' → 0
 * 'Z' → 25
 * 'a' → 0
 * 'z' → 25
 */

#include <stdio.h>

int letter_to_pos(char c)
{
    // Your code here
    return 0;
}

int main(void)
{
    printf("'A' → %d\n", letter_to_pos('A'));
    printf("'Z' → %d\n", letter_to_pos('Z'));
    printf("'a' → %d\n", letter_to_pos('a'));
    printf("'z' → %d\n", letter_to_pos('z'));
    return 0;
}
